function TestDOING( event )
	local caster = event.caster
	local target = event.target
	local ability = event.ability
	--caster:PerformAttack(target, bUseCastAttackOrb, bProcessProcs, bSkipCooldown, bIgnoreInvis, bUseProjectile) 
	--DoCleaveAttack(caster, target, ability, damage, radius, effect_name)

end